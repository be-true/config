{
  "name": "@be-true/config",
  "version": "0.0.2",
  "description": "Library for manage configuration for node.js",
  "main": "./dist/index.js",
  "types": "./dist/types/index.d.ts",
  "files": ["dist/*"],
  "scripts": {
    "build": "npm run clean && tsc",
    "test": "npm run clean && npm run build && metatests dist/test --dependentSubtests=true",
    "clean": "rm -rf ./dist",
    "my_publish": "npm run clean && npm run build && npm run test && npm publish"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/be-true/contract.git"
  },
  "keywords": [
    "config",
    "envs",
    "environments",
    "typescript"
  ],
  "author": "eawapownikov@gmail.com",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/be-true/config/issues"
  },
  "homepage": "https://github.com/be-true/config#readme",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^16.10.5",
    "metatests": "^0.7.2",
    "typescript": "^4.4.4"
  }
}
